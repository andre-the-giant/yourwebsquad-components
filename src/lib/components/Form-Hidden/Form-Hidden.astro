---
/*
Props:
- id?: string
- name?: string
- value?: string
Notes:
*/
const { id, name, value, ...rest } = Astro.props;

const resolvedId = id;
const resolvedName = name;
const resolvedValue = value ?? "";
---

<input type="hidden" id={resolvedId} name={resolvedName} value={resolvedValue} {...rest} />
